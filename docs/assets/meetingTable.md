# meetingTable

Long table with simple chairs around it and a tiny potted plant on top.

## Dimensions & origin

≈ 3.2 wide × 1.1 tall × 3 deep (with chairs). Origin at ground-center.
**Table top ≈ y 1.07** (× prop scale) — stack `deskLamp`/`mug` there.

## Options

| option | type | default |
| ------ | ---- | ------- |
| `seed` | number | 0 |
| `color` | color | `barkBrown` |
| `chairColor` | color | `sage` |
| `chairs` | number | 4 |

## Animations

`animations: none` — static asset.
